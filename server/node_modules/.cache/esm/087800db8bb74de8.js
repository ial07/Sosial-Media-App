let Post,User,expressjwt;_8bb‍.x([["requireSignin",()=>requireSignin],["canEditDeletePost",()=>canEditDeletePost],["isAdmin",()=>isAdmin]]);_8bb‍.w("../models/post",[["default",["Post"],function(v){Post=v}]]);_8bb‍.w("../models/user",[["default",["User"],function(v){User=v}]]);_8bb‍.w("express-jwt",[["expressjwt",["expressjwt"],function(v){expressjwt=v}]]);



       const requireSignin = expressjwt({
  secret: process.env.JWT_SECRET,
  algorithms: ["HS256"],
});

       const canEditDeletePost = async (req, res, next) => {
  try {
    const post = await Post.findById(req.params._id);
    // console.log("Can edit delete ", post);
    if (req.auth._id != post.postedBy) {
      return res.status(400).send("Unauthorized");
    } else {
      next();
    }
  } catch (err) {
    _8bb‍.g.console.log(err);
  }
};

       const isAdmin = async (req, res, next) => {
  try {
    const user = await User.findById(req.auth._id);
    if (user.role !== "Admin") {
      return res.status(400).send("Unauthorized");
    } else {
      next();
    }
  } catch (err) {
    _8bb‍.g.console.log(err);
  }
};
