let Post,User,cloudinary;_5f7‍.x([["posts",()=>posts],["getPost",()=>getPost],["createPost",()=>createPost],["uploadImage",()=>uploadImage],["newsFeeds",()=>newsFeeds],["userPost",()=>userPost],["updatePost",()=>updatePost],["deletePost",()=>deletePost],["likePost",()=>likePost],["UnLikePost",()=>UnLikePost],["addComment",()=>addComment],["removeComment",()=>removeComment],["totalPost",()=>totalPost]]);_5f7‍.w("../models/post",[["default",["Post"],function(v){Post=v}]]);_5f7‍.w("../models/user",[["default",["User"],function(v){User=v}]]);_5f7‍.w("cloudinary",[["default",["cloudinary"],function(v){cloudinary=v}]]);



cloudinary.config({
  cloud_name: process.env.CLOUDINARY_NAME,
  api_key: process.env.CLOUDINARY_KEY,
  api_secret: process.env.CLOUDINARY_SECRET,
});

       const posts = async (req, res) => {
  try {
    const posts = await Post.find()
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image")
      .sort({ createdAt: -1 })
      .limit(12);

    res.json(posts);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

       const getPost = async (req, res) => {
  try {
    const posts = await Post.findById(req.params._id)
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image");

    res.json(posts);
    _5f7‍.g.console.log(posts);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

       const createPost = async (req, res) => {
  //   console.log(req.body);
  const { content, image } = req.body;
  if (!content.length) {
    return res.json({
      error: "Please type anything...",
    });
  }
  // console.log("USERRR ", req.auth._id);
  try {
    const post = new Post({ content, image, postedBy: req.auth._id });
    post.save();
    res.json(post);
  } catch (err) {
    _5f7‍.g.console.log(err);
    res.sendStatus(400);
  }
};

       const uploadImage = async (req, res) => {
  // console.log("REQUEST FILE => ", req.files);
  try {
    const result = await cloudinary.uploader.upload(req.files.image.path);
    // console.log("UPLOAD IMAGE ", result);
    res.json({
      url: result.secure_url,
      public_id: result.public_id,
    });
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

// export const postsByUser = async (req, res) => {
//   try {
//     const posts = await Post.find()
//       .populate("postedBy", "_id name image")
//       .sort({ createdAt: -1 })
//       .limit(10);

//     res.json(posts);
//   } catch (err) {
//     console.log(err);
//   }
// };

       const newsFeeds = async (req, res) => {
  try {
    const user = await User.findById(req.auth._id);
    let following = user.following;
    following.push(req.auth._id);
    //paginantion
    const currentPage = req.params.page || 1;
    const perPage = 3;

    const posts = await Post.find({ postedBy: { $in: following } })
      .skip((currentPage - 1) * perPage)
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image")
      .sort({ createdAt: -1 })
      .limit(perPage);
    res.json(posts);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

       const userPost = async (req, res) => {
  try {
    const post = await Post.findById(req.params._id)
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image");
    // console.log("INI POST =>", post);
    res.json(post);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

       const updatePost = async (req, res) => {
  try {
    const post = await Post.findByIdAndUpdate(req.params._id, req.body, {
      new: true,
    });
    res.json(post);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

       const deletePost = async (req, res) => {
  try {
    const post = await Post.findByIdAndDelete(req.params._id);
    //remove the image from cloudinary
    if (post.image && post.image.public_id) {
      const image = await cloudinary.uploader.destroy(post.image.public_id);
    }
    res.json({ ok: true });
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

       const likePost = async (req, res) => {
  try {
    const post = await Post.findByIdAndUpdate(req.body._id, {
      $addToSet: { like: req.auth._id },
    });
    res.json(post);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};
       const UnLikePost = async (req, res) => {
  try {
    const post = await Post.findByIdAndUpdate(req.body._id, {
      $pull: { like: req.auth._id },
    });
    res.json(post);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

       const addComment = async (req, res) => {
  try {
    const { postId, comment } = req.body;
    const post = await Post.findByIdAndUpdate(
      postId,
      {
        $push: {
          comments: {
            text: comment,
            postedBy: req.auth._id,
          },
        },
      },
      { new: true }
    )
      .sort({ created: -1 })
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image");
    res.json(post);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

       const removeComment = async (req, res) => {
  try {
    const { postId, comments } = req.body;
    const post = await Post.findByIdAndUpdate(
      postId,
      {
        $pull: {
          comments: {
            _id: comments._id,
          },
        },
      },
      { new: true }
    );
    res.json(post);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};

       const totalPost = async (req, res) => {
  try {
    const total = await Post.find().estimatedDocumentCount();
    res.json(total);
  } catch (err) {
    _5f7‍.g.console.log(err);
  }
};
